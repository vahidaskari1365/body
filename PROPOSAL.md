# پروپوزال فنی — پلتفرم آنلاین مدیریت مربیان و ورزشکاران

> پاسخ فنی به شرح پروژه: مسیر اجرا، معماری، انتخاب تکنولوژی، راهکار دیتابیس/احراز هویت/ویدئو/پیام‌رسانی/اعلان، فازبندی و ملاحظات مقیاس‌پذیری.

---

## ۱) مسیر پیشنهادی اجرای پروژه

مسیر پیشنهادی من اجرای **MVP محصول‌محور در ۶ تا ۸ هفته** است، به این معنا که به‌جای ساختن همه‌چیز به‌صورت موازی، ابتدا «حلقه اصلی ارزش» را کامل و قابل استفاده می‌کنیم و سپس لایه‌های تجاری و پیشرفته را روی آن می‌نشانیم. حلقه اصلی ارزش این است: *مربی برنامه می‌سازد ← ورزشکار آن را می‌بیند و انجام می‌دهد ← نتیجه ثبت می‌شود ← مربی پیشرفت را می‌بیند.* تا وقتی این حلقه روان و بی‌نقص کار نکند، هیچ قابلیت دیگری ارزش نهایی ندارد.

دلایل انتخاب این مسیر:

- **کاهش ریسک محصولی:** با MVP زودهنگام می‌توانید با مربیان واقعی تست کنید و بازخورد بگیرید؛ تجربه نشان داده نیازهای واقعی مربیان (مثلاً کپی‌کردن سریع برنامه هفته قبل یا تمپلیت برنامه‌ها) فقط در استفاده واقعی مشخص می‌شود.
- **کنترل بودجه:** فازبندی شفاف یعنی هزینه هر فاز از قبل مشخص است و می‌توانید در انتهای هر فاز درباره ادامه تصمیم بگیرید.
- **آمادگی برای اپ موبایل از روز اول:** چون از ابتدا API-محور می‌سازیم، اپ موبایل بعداً فقط یک کلاینت جدید است؛ نه بازنویسی سیستم.

روی همین منطق، یک **دموی قابل استفاده از حلقه کامل** (داشبورد مربی، برنامه‌ساز، بانک حرکات با ویدئو، تقویم، ثبت نتایج توسط ورزشکار، چت، مالی و گزارش) پیاده شده که مبنای برآورد و گفتگوی فنی این پروپوزال است.

## ۲) معماری کلی سیستم

معماری انتخابی **مدولار مونولیت با API محور** است:

```
┌─────────────┐     ┌─────────────┐     ┌──────────────┐
│  Web App    │     │ Mobile App  │     │ (آینده) PWA  │
│  (Next.js)  │     │ (Expo/RN)   │     │              │
└──────┬──────┘     └──────┬──────┘     └──────┬───────┘
       │        REST / tRPC (JSON over HTTPS)  │
       └──────────────┬─────────────────────────┘
                      ▼
        ┌─────────────────────────────┐
        │   Backend (Next.js API /    │
        │   NestJS در صورت نیاز)      │
        │   Auth • Business Logic •   │
        │   Validation (Zod) • RBAC   │
        └───────┬──────────┬──────────┘
                │          │
      ┌─────────▼───┐   ┌──▼──────────────┐
      │ PostgreSQL  │   │ Object Storage  │
      │ (Prisma ORM)│   │ ویدئو (S3-compatible / │
      │             │   │ آرکان/مینئو ایرانی)    │
      └─────────────┘   └─────────────────┘
                │
      ┌─────────▼───────────┐
      │ Redis (کش، صف،      │
      │ rate-limit، پیام)   │
      └─────────────────────┘
```

چرا مونولیت مدولار و نه میکروسرویس؟ چون در شروع، میکروسرویس هزینه عملیاتی و پیچیدگی زیادی دارد و تیم کوچک را کند می‌کند. به‌جای آن کد را از ابتدا **به‌صورت ماژول‌های دامنه‌محور** (users، programs، exercises， messaging، payments، reports) با مرزهای مشخص می‌نویسیم تا در آینده اگر سرویسی (مثلاً پردازش ویدئو) نیاز به مقیاس مستقل داشت، جداسازی‌اش کم‌هزینه باشد.

**نکته مهم:** سمت کلاینت، اپلیکیشن به‌صورت SPA با ناوبری سمت کلاینت پیاده شده که تجربه کاربری اپ‌مانند (بدون ری‌لود صفحه) می‌دهد — همین ساختار برای پنل‌های داشبوردی مثل این پروژه ایده‌آل است و بعداً به‌راحتی به PWA هم تبدیل می‌شود.

## ۳) تکنولوژی‌ها و ابزارهای هر بخش

| بخش | انتخاب | چرا؟ |
|------|---------|------|
| فرانت‌اند | **Next.js 16 + TypeScript + Tailwind CSS + shadcn/ui** | اکوسیستم پایدار، SEO برای صفحات عمومی، دیزاین‌سیستم آماده، استخدام‌پذیری آسان نیرو |
| نمودارها | **Recharts** | سبک، RTL-فرندلی، کافی برای گزارش‌های عملکرد |
| بک‌اند | **API Routes همان Next.js** (در مقیاس بالاتر: NestJS) | یک زبان (TypeScript) در کل استک، اشتراک تایپ‌ها با فرانت، سرعت توسعه |
| اعتبارسنجی داده | **Zod** | شمای واحد برای Validation فرانت و بک |
| ORM | **Prisma** | مایگریشن تمیز، Type-Safety کامل، پشتیبانی PostgreSQL/MySQL |
| دیتابیس | **PostgreSQL** | رابطه‌ای، JSONB برای داده‌های انعطاف‌پذیر، مقیاس‌پذیر و بالغ |
| کش/صف | **Redis + BullMQ** | صف پردازش ویدئو، اعلان‌ها، rate-limit |
| اپ موبایل (آینده) | **Expo / React Native** | اشتراک منطق و تایپ‌ها با وب، خروجی iOS و Android هم‌زمان |
| زیرساخت | Docker + VPS ایرانی یا ابر (آروان/لیارا/سرو) + CI/CD ساده | انطباق با الزامات داخلی، هزینه بهینه |

## ۴) راهکارهای زیرساختی

### دیتابیس
**PostgreSQL با Prisma ORM.** اسکیمای دیتابیس از همین حالا نرمال و رابطه‌ای طراحی شده: کاربران و نقش‌ها، پروفایل ورزشکار، حرکات، برنامه‌ها، جلسات، ریز-حرکات جلسه، لاگ تمرین، اندازه‌گیری‌های بدنی، پیام‌ها و تراکنش‌های مالی (نسخه دموی پیوست‌شده دقیقاً همین اسکیما را با ۹+ مدل پیاده و سید کرده است). این مدل‌بندی یعنی گزارش‌گیری و تحلیل پیشرفته بعداً نیاز به بازطراحی داده ندارد. برای گزارش‌های سنگین در آینده از Read-Replica یا Data Warehouse سبک استفاده می‌کنیم؛ نه تغییر معماری.

### احراز هویت
**سشن JWT کوتاه‌عمر + Refresh Token با کوکی HttpOnly** (پیاده‌سازی فعلی دمو با سشن سمت سرور است که همان تضمین‌های امنیتی را ساده‌تر می‌دهد). نقش‌ها: `COACH` و `ATHLETE` با RBAC در لایه API — یعنی حتی اگر کسی endpoint ورزشکار را صدا بزند، سمت سرور مالکیت داده بررسی می‌شود (هر ورزشکار فقط به داده خودش و هر مربی فقط به ورزشکاران خودش دسترسی دارد). ثبت‌نام با موبایل + OTP (پیامک ایرانی مثل کاوه‌نگار) برای بازار ایران طبیعی‌تر از ایمیل است و در فاز ۲ اضافه می‌شود. ورود با گوگل/اپل برای اپ موبایل در نقشه راه است.

### ذخیره ویدئو
ویدئو **هرگز در دیتابیس یا دیسک سرور** نگه‌داری نمی‌شود:
- **گزینه صفر (MVP):** مربی لینک ویدئو از آپارات/یوتیوب می‌دهد — همین حالا در دمو پیاده شده و پخش‌کننده هوشمند هر سه حالت (آپارات، یوتیوب، mp4) را ساپورت می‌کند.
- **گزینه استاندارد:** آپلود مستقیم مرورگر به **Object Storage سازگار با S3** (آروان ابری برای ایران، یا MinIO سلف‌هاست) با Pre-signed URL؛ سپس صف پردازش (BullMQ + ffmpeg) transcode به چند کیفیت HLS می‌کند تا پخش روی اینترنت ضعیف موبایل هم روان باشد.
- محدودیت حجم/فرمت در لایه API + اسکن ساده فایل.

### پیام‌رسانی
پیام مربی↔ورزشکار به‌صورت **REST + Polling سبک** در MVP (همین حالا در دمو کار می‌کند: لیست گفتگوها، شمارنده نخوانده‌ها، ارسال/دریافت). در فاز ۲ مهاجرت به **WebSocket (Socket.IO)** برای تحویل آنی، تیک خوانده‌شده و typing است — لایه API از اول طوری نوشته شده که تغییر transport صرفاً سمت سرور است. پیام‌ها در PostgreSQL ذخیره می‌شوند (قابلیت جستجو و آرشیو) و برای بهینه‌سازی، آخرین گفتگوها در Redis کش می‌شوند.

### اعلان‌ها
سه لایه، به‌ترتیب اولویت:
1. **درون‌اپ:** بج و لیست اعلان (برنامه جدید، پیام جدید، تمرین ثبت‌شده) — ارزان و مؤثر.
2. **پیامک:** برای رویدادهای مهم (برنامه جدید، یادآوری جلسه) با سرویس ایرانی.
3. **Push (فاز اپ موبایل):** Firebase یا P8/Push برای iOS — از همان صف اعلان متمرکز (events + subscribers) تغذیه می‌شود تا منطق «چه کسی چه اعلانی می‌گیرد» فقط یک‌بار نوشته شود.

## ۵) فازبندی و ترتیب اجرا

| فاز | محتوا | خروجی قابل تحویل | بازه تقریبی |
|------|--------|--------------------|--------------|
| **۰ — طراحی و پایه** | برندهای محصول، طراحی UX جریان‌های اصلی، دیزاین‌سیستم، اسکیمای دیتابیس، بوت‌استرپ ریپو و CI | فیگمای اولیه + ریپوی آماده | ۱ هفته |
| **۱ — حلقه اصلی (MVP)** | احراز هویت + پنل مربی (ورزشکاران، بانک حرکات، برنامه‌ساز، ارسال برنامه) + پنل ورزشکار (مشاهده برنامه، ثبت نتایج) | نسخه قابل تست با مربی واقعی | ۳–۴ هفته |
| **۲ — تعامل و نگه‌داشت** | تقویم جلسات، پیام‌رسانی زنده (Socket.IO)، اعلان درون‌اپ + پیامک، روند پیشرفت و گزارش‌ها، ثبت اندازه‌گیری بدنی | محصول کامل برای ۱۰–۲۰ مربی پایلوت | ۲–۳ هفته |
| **۳ — تجاری‌سازی** | مدیریت مالی/شهریه، درگاه پرداخت (زرین‌پال/آیدی‌پی)، پلن‌های اشتراک مربی، لندینگ عمومی | نسخه درآمدزا | ۲–۳ هفته |
| **۴ — اپ موبایل** | Expo/RN روی همان API + Push Notification + انتشار کافه‌بازار و استور | اپ iOS/Android | ۴–۶ هفته |
| **۵ — تحلیل پیشرفته** | مقایسه دوره‌ای، هوش تمرینی، خروجی اکسل/PDF، داشبورد پیشرفته مربی | ویژگی‌های رقابتی | مستمر |

ترتیب فازها عمداً طوری است که **هر فاز خودش ارزش مستقل دارد**؛ اگر در هر نقطه پروژه متوقف شود، شما یک محصول کاری دارید.

## ۶) ملاحظاتی که از روز اول برای آینده رعایت می‌کنیم

1. **API-First و Stateless:** همه منطق‌ها سمت سرور و پشت REST API؛ اپ موبایل و وب دقیقاً از یک API می‌خورند. هیچ منطق حیاتی در فرانت نوشته نمی‌شود.
2. **Type-Safety سرتاسری:** تایپ‌های TypeScript مشترک بین فرانت/بک (با Zod و Prisma Types) — یعنی تغییر اسکیما قبل از اجرا خطا می‌دهد، نه در پروداکشن.
3. **مالکیت داده و Multi-tenancy منطقی:** همه کوئری‌ها به `coachId`/`athleteId` مقید می‌شوند (در دموی پیوست هم دقیقاً همین‌طور است) تا هم امنیت تضمین شود و هم بعداً قابلیت «باشگاه/آکادمی با چند مربی» به‌راحتی اضافه شود.
4. **دیتابیس رابطه‌ای نرمال از ابتدا:** افزودن جدول «اشتراک‌ها»، «یادآوری‌ها» یا «تگ حرکات» بعداً فقط مایگریشن است، نه بازطراحی.
5. **فایل‌ها بیرون از سرور:** ویدئو/تصاویر از روز اول روی Object Storage — یعنی افزودن سرور دوم برای مقیاس، بدون درد مهاجرت فایل‌ها.
6. **صف کارها (Background Jobs) از فاز ۲:** هر کار سنگین (transcode، ایمیل/پیامک انبوه، گزارش‌گیری) از همان اول در صف می‌رود تا آپ‌تایم سرور حفظ شود.
7. **مشاهده‌پذیری (Observability):** لاگ ساخت‌یافته، مانیتورینگ خطا (مثل Sentry) و متریک‌ها از فاز ۱؛ تصمیم‌های مقیاس‌دهی بر اساس داده، نه حدس.
8. **تست و CI:** تست API مسیرهای حیاتی (ثبت‌نام، ارسال برنامه، ثبت لاگ) + دیپلوی خودکار با هر merge؛ ریسک رگرسیون نزدیک صفر.
9. **RTL و فارسی به‌عنوان شهروند درجه‌یک:** تاریخ شمسی، اعداد فارسی، فونت وزیرمتن و چیدمان راست‌چین از ابتدا — بازطراحی بعدی لازم ندارد (در دمو پیاده شده).
10. **امنیت پایه:** Rate-limit، اعتبارسنجی سمت سرور همه ورودی‌ها، کوکی‌های HttpOnly، و اصل حداقل‌دسترسی در همه endpointها.

---

### جمع‌بندی

این مسیر یک انتخاب «ترند» نیست؛ ترکیبی از ابزارهای بالغ، یک معماری ساده اما قابل رشد، و فازبندی کم‌ریسک است که **در ۳ تا ۴ هفته اول یک محصول قابل تست واقعی** تحویل می‌دهد و در ماه‌های بعد بدون بازنویسی به اپ موبایل و قابلیت‌های تجاری می‌رسد. دموی پیوست‌شده گواه عملی همین ادعاست: همین اسکیمای داده و همین معماری API، مبنای توسعه نسخه نهایی خواهد بود.

*(نسخه دموی قابل اجرا در همین ریپوزیتوری قرار دارد — README را ببینید.)*

---

# پیوست: چطور به این سؤال‌ها جواب بدهید؟

این بخش برای **شما**ست، نه کارفرما. قبل از جلسه/مکاتبه بخوانید تا با اعتمادبه‌نفس و یکدست پاسخ بدهید.

## قانون طلایی

هر جواب را در سه لایه بدهید: **جواب کوتاه (۲ جمله) ← دلیل فنی (۱ جمله) ← دموی واقعی (نشان دهید).** دمو قوی‌تر از هر توضیحی است؛ تا می‌توانید صحبت را به صفحه‌ی دمو ببرید.

## پاسخ‌های آماده به ۶ سؤال

### سؤال ۱ — «چه مسیری پیشنهاد می‌کنی؟»
**جواب کوتاه:** «MVP محصول‌محور در ۶ تا ۸ هفته؛ اول حلقه‌ی کامل ارزش (برنامه ← اجرا ← ثبت ← گزارش) را روان می‌کنیم، بعد لایه‌های تجاری و پیشرفته.»
**نکته تکان‌دهنده:** اگر پرسیدند «چرا همه‌چیز را یکجا نمی‌سازی؟» بگویید: «چون نیازهای واقعی مربی‌ها را فقط با استفاده‌ی واقعی می‌فهمیم؛ MVP یعنی پول شما صرف چیزهایی شود که واقعاً استفاده می‌شود.»
**نشان دهید:** دمو را باز کنید — همین حلقه الان کار می‌کند.

### سؤال ۲ — «معماری را چطور طراحی می‌کنی؟»
**جواب کوتاه:** «مونولیت مدولار با API محور؛ همه‌چیز پشت یک API استاندارد تا اپ موبایل بعداً فقط یک کلاینت جدید باشد، نه بازنویسی.»
**نکته تکان‌دهنده:** اگر پرسیدند «چرا میکروسرویس نه؟» بگویید: «میکروسرویس برای تیم کوچک یعنی هزینه‌ی عملیاتی بیشتر و سرعت کمتر. مرزهای ماژول‌ها را از اول درست می‌گذاریم تا هر وقت لازم شد، جداسازی ارزان باشد.»
**هشدار:** وارد جزئیات دیاگرام نشوید مگر اینکه طرف فنی باشد؛ دیاگرام معماری را نشان دهید و بگذارید سؤال بپرسد.

### سؤال ۳ — «چه تکنولوژی‌هایی؟»
**جواب کوتاه:** «Next.js + TypeScript در کل استک، PostgreSQL با Prisma، Redis برای صف و کش، و برای اپ موبایل Expo/RN روی همین API.»
**نکته تکان‌دهنده:** «انتخاب‌های من بر اساس سه معیار است: بالغ بودن (bug کمتر)، پیدا شدن نیرو (ریسک پروژه کمتر)، و اشتراک تایپ‌ها بین فرانت/بک/موبایل (سرعت بیشتر، خطای کمتر). تکنولوژی ابزار است، نه مد؛ من دنبال پایدارترین گزینه‌ام، نه جدیدترین.»

### سؤال ۴ — «دیتابیس، احراز هویت، ویدئو، پیام، اعلان؟»
**جواب کوتاه (هر کدام یک جمله):**
- دیتابیس: «PostgreSQL رابطه‌ای و نرمال — گزارش و تحلیل پیشرفته بعداً به بازطراحی نیاز ندارد.»
- احراز هویت: «سشن با کوکی HttpOnly و RBAC سمت سرور؛ هر کاربر فقط به داده‌ی خودش می‌رسد — حتی با دستکاری درخواست.»
- ویدئو: «هرگز روی دیسک سرور؛ Object Storage سازگار S3 با پخش HLS — و در MVP حتی لینک آپارات کافی است، صرفه‌جویی هوشمند.»
- پیام: «MVP با REST، فاز بعد WebSocket؛ transport قابل تعویض است چون API از اول تمیز نوشته شده.»
- اعلان: «سه لایه: درون‌اپ، پیامک، و بعداً Push — همه از یک صف متمرکز تا منطق فقط یک‌بار نوشته شود.»
**نکته تکان‌دهنده:** جمله‌ی «ویدئو هرگز در دیتابیس ذخیره نمی‌شود» را حتماً بگویید؛ نشانه‌ی تجربه است و کارفرماها به آن واکنش خوبی نشان می‌دهند.

### سؤال ۵ — «فازبندی چطوره؟»
**جواب کوتاه:** «۶ فاز، هر فاز خروجی قابل استفاده دارد: پایه و طراحی (۱ هفته) ← MVP (۳–۴ هفته) ← تعامل و گزارش (۲–۳ هفته) ← تجاری‌سازی (۲–۳ هفته) ← اپ موبایل (۴–۶ هفته) ← تحلیل پیشرفته.»
**نکته تکان‌دهنده:** «هر فاز خودش ارزش مستقل دارد؛ یعنی اگر هر جا متوقف شویم، شما همیشه یک محصول کاری دارید — نه نیمه‌کاره.»
**هشدار:** روی تاریخ‌ها قفل نشوید؛ بگویید «بازه‌ی تقریبی وابسته به سرعت بازخورد شماست.»

### سؤال ۶ — «از الان برای آینده چی رعایت می‌کنی؟»
**جواب کوتاه:** «سه چیز اصلی: API-First (اپ موبایل = کلاینت جدید)، مالکیت داده با coachId در همه‌ی کوئری‌ها (امنیت + چند-مربی بعداً)، و فایل‌ها بیرون از سرور (مقیاس بدون درد مهاجرت).»
**نکته تکان‌دهنده:** «تاریخ شمسی، RTL و فارسی را هم از روز اول درست پیاده می‌کنیم — این‌ها اگر بعداً بخواهند اضافه شوند، بازطراحی کامل UI است.» لیست ۱۰گانه‌ی بخش ۶ پروپوزال را برایشان بفرستید.

## سؤال‌هایی که خودتان باید بپرسید (نشانه‌ی حرفه‌ای بودن)

1. «تعداد مربیانی که در ۶ ماه اول انتظار دارید؟» → روی اولویت‌بندی فاز ۲ اثر دارد.
2. «پرداخت‌ها از کدام درگاه و با چه مدل درآمدی؟ (شهریه‌ی مستقیم مربی، کمیسیون، اشتراک)» → فاز ۳.
3. «ویدئوها را خودتان دارید یا باید ضبط شود؟» → ریسک پنهان محتوا.
4. «چه کسی در طرف شما Product Owner است و هر هفته چند ساعت وقت دارد؟» → بدون مالک محصول، پروژه‌های این‌چنینی لنگ می‌زنند.
5. «انتظار دارید تا کِی اولین مربی واقعی استفاده کند؟» → انتظارات را از همان اول با فازبندی هم‌راستا کنید.

## هشدارهای مذاکره

- **قرارداد را فاز-به-فاز ببندید** با خروجی و پذیرش مشخص برای هر فاز — نه یک قرارداد بزرگ مبهم.
- **حدود کار (Scope) را مکتوب کنید:** «پنل مربی شامل این ۱۰ قابلیت» — هر چیز بیرون از آن، تغییرات (Change Request) با هزینه‌ی جدا.
- **دمو را نسپارید:** بگویید «دمو روی سرور من قابل نمایش است»؛ کد را فقط بعد از قرارداد/پیش‌پرداخت تحویل دهید.
- **پیش‌پرداخت ۳۰–۴۰٪** برای فاز اول استاندارد است؛ تسویه‌ی هر فاز قبل از شروع فاز بعد.
- **قول تاریخ قطعی ندهید** قبل از دسترسی به محتوای واقعی (ویدئوها، تعداد حرکات) و تصمیم‌های برندینگ کارفرما.
